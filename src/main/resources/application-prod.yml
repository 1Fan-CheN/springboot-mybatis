spring:
  datasource:
    write:
      url: jdbc:mysql:///ad_monitor_db?db_consul=toutiao.mysql.ad_monitor_db_write&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&auth_enable=true&useSSL=false&serverTimezone=UTC
    read:
      url: jdbc:mysql:///ad_monitor_db?db_consul=toutiao.mysql.ad_monitor_db_write&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&auth_enable=true&useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
    initialization-mode: always
    continue-on-error: true
    hikari:
      minimum-idle: 5
      connection-test-query: SELECT 1
      maximum-pool-size: 20
      auto-commit: true
      idle-timeout: 30000
      max-lifetime: 60000
      connection-timeout: 30000
